<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.ClassBoardMapper">

<select id="getList" resultType="org.zerock.domain.ClassBoardVO">
	<![CDATA[
		select a.bno, a.className, a.classAge, b.userName, a.classTotal
		from tb_class a, tb_teacher b
		where a.className = b.className
	]]>
</select>

 	<insert id="insert">
		insert into tb_class (bno, className, classAge, userName, classTotal)
		values (seq_tb_class.nextval, #{className}, #{classAge}, #{userName}, #{classTotal})
	</insert>
	
	<insert id="insertSelectKey">

		<selectKey keyProperty="bno" order="BEFORE"
			resultType="long">
			select seq_tb_class.nextval from dual
		</selectKey>

		insert into tb_class (bno, className, classAge, userName, classTotal)
		values (#{bno}, #{className}, #{classAge}, #{userName},  #{classTotal})
	</insert>


 	<select id="read" resultType="org.zerock.domain.ClassBoardVO">
		select * from tb_class where bno = #{bno}
	</select>
	

	<delete id="delete">
		delete from tb_class where bno = #{bno}
	</delete>
	
	
	<update id="update">
	    update tb_class
	    set className= #{className},
	    classAge=#{classAge},
	    userName=#{userName},
	    classTotal = #{classTotal}
	    where bno = #{bno}
	 </update>


</mapper>