<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.QuestionsBoardMapper">

<select id="getList" resultType="org.zerock.domain.QuestionsBoardVO">
	<![CDATA[
		select * from tb_qna where ano > 0
	]]>
</select>

 	<insert id="insert">
		insert into tb_apply (bno, 
		values (seq_tb_apply.nextval,

	</insert>

 	<select id="read" resultType="org.zerock.domain.ApplyBoardVO">
		select * from tb_qna where bno = #{bno}
	</select>
	

	<delete id="delete">
		delete from tb_qna
		where bno = #{bno}
		<!-- and pw = #{pw} -->
	</delete>
	
	
	<update id="update">
	    update tb_apply
	    set 	pname = #{pname}
				,relation = #{relation}
				,pbirth = #{pbirth}
				,addr = #{addr}
				,phone = #{phone}
				,cname = #{cname}
				,cgender = #{cgender}
				,cbirth = #{cbirth}
				,apptype = #{apptype}
				,cunicode = #{cunicode}
		where ano = #{ano}
		<!-- and pw = #{pw} -->
	 </update>
	 
	 <update id="pass">
	 {call
	 	declare
	 	begin
			insert into tb_child(cunicode, classname, cname, cbirth)
			values(#{cunicode}, #{cclass}, #{cname}, #{cbirth});
			
			update tb_apply
			set		cunicode = #{cunicode}
					,cclass = #{cclass}
					,enter = #{enter}
					,graduation = #{graduation}
			where ano = #{ano};
		end
	 }
	 </update>
	 
</mapper>